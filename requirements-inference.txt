# Minimal requirements for inference API (CPU-only)
# Use --index-url for CPU-only PyTorch to avoid 3GB+ CUDA packages
--index-url https://download.pytorch.org/whl/cpu
torch
numpy
fastapi
uvicorn
pydantic
